# Prepared Ligands

Store docking-ready ligand files here.

Examples:

- protonated reference ligand files
- analog `PDBQT` files
- per-ligand metadata needed for reproducible docking
- `analog_library/` for the first-round vamifeport analog set generated from
  `data/reference/analog_designs.csv`
